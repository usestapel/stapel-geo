{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "geo.nearby",
  "description": "Proximity search over the location tree (top-K nearest). Provide either coordinates (lat+lon, optional precision) or a geohash. Returns {\"results\": [{uuid, name, country, geohash, distance_km}, ...]} nearest-first. distance_km is the exact great-circle (haversine) distance in km.",
  "type": "object",
  "properties": {
    "lat": {"type": "number", "minimum": -90, "maximum": 90, "description": "Latitude (with lon)."},
    "lon": {"type": "number", "minimum": -180, "maximum": 180, "description": "Longitude (with lat)."},
    "geohash": {"type": "string", "pattern": "^[0-9bcdefghjkmnpqrstuvwxyz]{1,12}$", "description": "Geohash prefix (alternative to lat/lon)."},
    "precision": {"type": "integer", "minimum": 1, "maximum": 12, "description": "Geohash precision when searching by coordinates."},
    "limit": {"type": "integer", "minimum": 1, "description": "Maximum number of results."}
  },
  "oneOf": [
    {"required": ["lat", "lon"]},
    {"required": ["geohash"]}
  ],
  "additionalProperties": false
}
