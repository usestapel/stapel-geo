{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "geo.geohash_encode",
  "description": "Encode a coordinate pair to a geohash string (pure arithmetic, no database). Returns {\"geohash\": \"<str>\"}. Consumers (e.g. listings) call this to stamp geohashes onto their own rows without importing stapel_geo.",
  "type": "object",
  "properties": {
    "lat": {"type": "number", "minimum": -90, "maximum": 90, "description": "Latitude."},
    "lon": {"type": "number", "minimum": -180, "maximum": 180, "description": "Longitude."},
    "precision": {"type": "integer", "minimum": 1, "maximum": 12, "description": "Geohash length (default: STAPEL_GEO[\"GEOHASH_PRECISION\"])."}
  },
  "required": ["lat", "lon"],
  "additionalProperties": false
}
