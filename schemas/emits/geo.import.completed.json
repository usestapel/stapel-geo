{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "geo.import.completed",
  "description": "Emitted when a GADM GeoFile finishes importing (async path). Consumers (search reindex, caches) subscribe by name; at-least-once, must be idempotent.",
  "type": "object",
  "properties": {
    "geofile_id": {"description": "Primary key of the imported GeoFile."},
    "location_level": {"type": "integer", "minimum": 0, "description": "GADM depth level imported (0 = country)."},
    "feature_count": {"type": "integer", "minimum": 0, "description": "Number of features imported."},
    "status": {"type": "string", "const": "completed", "description": "Terminal status marker."}
  },
  "required": ["geofile_id", "location_level", "feature_count", "status"],
  "additionalProperties": false
}
